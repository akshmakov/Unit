<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="digi.list">
      <field name="name">digi list</field>
      <field name="model">digi.digi</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="digi.action_window">
      <field name="name">digi window</field>
      <field name="res_model">digi.digi</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="digi.action_server">
      <field name="name">digi server</field>
      <field name="model_id" ref="model_digi_digi"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="digi" id="digi.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="digi.menu_1" parent="digi.menu_root"/>
    <menuitem name="Menu 2" id="digi.menu_2" parent="digi.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="digi.menu_1_list" parent="digi.menu_1"
              action="digi.action_window"/>
    <menuitem name="Server to list" id="digi" parent="digi.menu_2"
              action="digi.action_server"/>
    -->
  </data>
</odoo>